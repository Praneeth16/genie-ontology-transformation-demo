[project]
name = "genie-ontology-transformation-demo"
version = "0.1.0"
description = "A Databricks Genie Ontology demo for an enterprise transformation value office."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "databricks-sdk>=0.77.0",
]

[dependency-groups]
dev = [
    "ruff==0.16.7",
    "pyyaml>=6.0",
]

[tool.ruff]
line-length = 100
