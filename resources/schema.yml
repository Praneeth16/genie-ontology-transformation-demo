resources:
  schemas:
    transformation_demo:
      catalog_name: ${var.catalog}
      name: ${var.schema}
      comment: >-
        Synthetic enterprise transformation value office data for the Genie
        Ontology demo. It contains no customer data.
